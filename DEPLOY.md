# 🚀 Инструкция по развертыванию StroyUchet SaaS

Это руководство поможет вам развернуть приложение на любом Linux VPS (Ubuntu/Debian) за несколько минут.

## 📋 Требования
1.  **VPS сервер** (Ubuntu 22.04 рекомендую).
2.  **Домен** (желательно), направленный на IP сервера.
3.  Установленные **Docker** и **Docker Compose**.

## 🛠 Подготовка сервера

### 1. Установка Docker
Выполните эти команды на сервере:
```bash
# Обновление пакетов
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Права для пользователя (чтобы не писать sudo каждый раз)
sudo usermod -aG docker $USER
newgrp docker
```

### 2. Загрузка проекта
```bash
# Клонируем репозиторий (или копируем файлы)
git clone <your-repo-url> stroyuchet
cd stroyuchet

# Создаем файл настроек
cp .env.example .env
nano .env
# !ВАЖНО: Заполните .env реальными данными (SECRET, API KEYS)!
```

## 🚀 Запуск
Одной командой мы запускаем Базу Данных, Бэкенд, Фронтенд и Nginx:

```bash
docker compose up -d --build
```
*Флаг `-d` запускает в фоновом режиме, `--build` собирает контейнеры.*

## 🔄 Оновление (Как вносить изменения)
Когда вы внесли изменения в код и отправили их в Git:

1.  **Скачайте обновления**:
    ```bash
    git pull
    ```
2.  **Перезапустите одной командой**:
    ```bash
    docker compose up -d --build
    ```
    
Docker сам пересоберет только изменившиеся части и перезапустит контейнеры. **Это всё!** Никаких ручных `npm install` или `build`.

## 🔍 Полезные команды

**Посмотреть логи (все сразу):**
```bash
docker compose logs -f
```

**Посмотреть логи конкретного сервиса:**
```bash
docker compose logs -f backend
# или
docker compose logs -f frontend
```

**Остановить всё:**
```bash
docker compose down
```

## 🌐 Настройка Nginx (Порты)
По умолчанию приложение работает на **порту 80** (http://your-ip).
- Бэкенд доступен внутри по адресу `http://backend:4000`.
- Фронтенд доступен внутри по адресу `http://frontend:3000`.
- Reverse Proxy (Nginx) перенаправляет запросы:
  - `/api/*` -> Backend
  - `/*` -> Frontend

Приложение готово к работе!
