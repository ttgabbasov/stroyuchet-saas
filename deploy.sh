#!/bin/bash

# Навигация в директорию проекта (подставьте правильный путь, если он другой)
cd /root/stroyuchet-saas

echo "🚀 Начинаю деплой..."

# Подтягиваем последние изменения
echo "📥 Получаю обновления из GitHub..."
git pull origin main

# Пересобираем и запускаем контейнеры
echo "🏗 Пересобираю образы и перезапускаю сервисы..."
docker compose up -d --build

# Очистка старых неиспользуемых образов (чтобы не забивать диск)
echo "🧹 Очистка старых Docker-образов..."
docker image prune -f

echo "✅ Деплой успешно завершен!"
