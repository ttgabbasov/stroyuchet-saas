# 🏗 СтройУчёт (StroyUchet SaaS)

**Финансовый интеллект вашей стройки 💎**

Профессиональный SaaS-инструмент для управления финансами строительных объектов. Превращает хаос отчетности в четкую систему принятия решений.

## 🚀 Киллер-фичи

### 🎙 AI-Голосовой ввод (Нейро-прораб)
Больше не нужно вводить данные вручную. Просто скажите боту:
> *"Купил 20 мешков цемента на 8000 рублей для фундамента"*
ИИ автоматически извлечет сумму, категорию и объект.
*(Требуется настройка `GOOGLE_API_KEY`)*

### 📸 OCR-сканер чеков
Пришлите фото чека (с QR-кодом), и система автоматически считает сумму и дату.

### 🔄 Взаиморасчеты (Equity)
Автоматический расчет долей партнеров (50/50). Система показывает, кто сколько вложил, сколько вывел, и кто кому должен для выравнивания баланса.

### 🕵️ Глубокий аудит
Полная история всех действий (кто, когда, с какого IP). Защита от "тихих" удалений.

## 🛠 Технологический стек
-   **Backend**: Node.js, Express, TypeScript
-   **Database**: PostgreSQL, Prisma ORM
-   **Bot**: Telegraf (Telegram Bot API)
-   **Frontend**: Next.js, TailwindCSS
-   **AI**: Google Gemini API

## ⚙️ Установка и запуск

### 1. Требования
-   Node.js (v18+)
-   PostgreSQL (или доступ к БД)

### 2. Установка зависимостей
```bash
npm install
cd frontend && npm install && cd ..
```

### 3. Настройка окружения
Создайте файл `.env` в корне проекта (см. пример в корн):
```env
# Server
PORT=4000
DATABASE_URL="postgresql://user:pass@localhost:5432/stroyuchet"
JWT_SECRET="your_super_secret_jwt_key_min_32_chars"

# Telegram
TELEGRAM_BOT_TOKEN="your_bot_token"

# AI (Обязательно для голосового ввода)
GOOGLE_API_KEY="your_gemini_api_key"

# Storage
UPLOAD_DIR="./uploads" 
```

### 4. Запуск
```bash
# Запуск БД миграций
npx prisma migrate deploy

# Запуск сервера (Backend + Bot)
npm run dev

# Запуск Frontend (в отдельном терминале)
cd frontend && npm run dev
```

## 🎭 Ролевая модель
-   **OWNER**: Полный доступ.
-   **PARTNER**: Просмотр аналитики, взаиморасчеты.
-   **FOREMAN** (Прораб): Только свои объекты и касса.
-   **ACCOUNTANT**: Отчеты и цифры.
-   **VIEWER**: Только просмотр.
